# Vite frontend — copy to .env.local if you need overrides.
# Leave VITE_API_URL unset for local dev: requests go to /api and Vite proxies to http://127.0.0.1:5000.
# VITE_API_URL=https://your-api.example.com/api
